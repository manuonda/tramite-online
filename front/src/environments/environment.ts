/**
 * Production Environment Configuration
 */

export const environment = {
  production: true,
  apiUrl: 'https://api.tramite-online.com/api',
  tokenKey: 'auth_token',
  refreshTokenKey: 'refresh_token',
};
